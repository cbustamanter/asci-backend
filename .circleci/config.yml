version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:16.10
    steps:
      - checkout
      - setup_remote_docker:
          version: 19.03.13
      - run:
          name: Build and push Docker image
          command: |            
            docker build -t asciperu/$IMAGE_NAME:8 .
            echo $DOCKER_PWD | docker login -u $DOCKER_LOGIN --password-stdin
            docker push asciperu/$IMAGE_NAME:$TAG